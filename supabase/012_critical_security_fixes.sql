-- ============================================================================
-- Colecciona 012: Historical migration (SUPERSEDED by schema.sql)
-- All fixes are now included in the definitive schema.sql
-- This file is kept for reference only. DO NOT RUN — use schema.sql instead.
-- ============================================================================
-- Fixes included in schema.sql:
-- 1. trade_history: auto-generated by trigger (no client INSERT)
-- 2. trade_proposals: granular RLS + immutable field protection + state machine
-- 3. trade_proposal_items: validates quantity <= duplicates + committed tracking + role
-- 4. collection_items: cumulative quantity model with CHECK constraints
-- 5. reserve_products_for_checkout: auth.uid() = buyer check
-- 6. wallet: SELECT-only for users, no INSERT policy
-- 7. wallet_transactions: audit trail table
-- 8. reviews: UNIQUE(order_id, reviewer_id), create_review RPC
-- 9. user_private: granular SELECT/INSERT/UPDATE policies
-- ============================================================================
SELECT '012 is historical — use schema.sql instead' AS notice;
